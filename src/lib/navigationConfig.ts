/**
 * On 24.02.2019 Tonny had a weird animation bug where screens disappeared when navigating out of them,
 * we haven't traced it back to any root cause yet.
 * Not sure if this is the right solution, but we've tried persisting the navigators.
 *
 */
export const shouldPersistViews = true;
